<%= form_tag welcome_index_path, :method => 'get' do %>
  <p>
  	Busca:
  	<%= collection_select(:categoria, :id, Categoria.all, :id, :nome)  %>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Buscar", :name => nil %>
  </p>
<% end %>